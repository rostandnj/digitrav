{% extends 'client/base/base.html.twig' %}

{% block main %}

    <section class=" h-100vh" >

        <div class="home-table">
            <div class="home-table-center">
                <div class="container">
                    <div class="row">
                        <div class="col-lg-12" >
                            <div class="text-white text-center">
                                <div class="home_divider mb-4 mx-auto"></div>
                                <h1 style="color: black" class="header_title pt-2 mb-0 font-weight-bold mx-auto"><span class="fonts-italic font-italic font-weight-normal">Please Login before proceed</span></h1>
                                <p class="header_subtitle pt-4 mb-0 pb-2 text-white mx-auto"> </p>
                                <div class="header_btn">
                                    <a href="{{ path('web_login') }}" class="btn btn-custom mt-4">{{ 'login'|trans }}</a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}
